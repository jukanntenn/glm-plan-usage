---
phase: 02-close-confidence-gaps-runtime-behavior
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: ["CLAUDE.md"]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "CLAUDE.md documentation reflects that timeout_ms and retry_attempts in ApiConfig are actually usable"
    - "Documentation matches current implementation after config/runtime alignment"
    - Only targeted changes where drift existed - no full documentation rewrite (D-08)
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated documentation for ApiConfig configuration"
      contains: "timeout_ms"
      contains: "retry_attempts"
  key_links:
    - from: "CLAUDE.md Configuration Extension Pattern"
      to: "src/config/types.rs ApiConfig"
      pattern: "timeout_ms.*default.*5000.*retry_attempts.*default.*2"
---

<objective>
Update documentation in CLAUDE.md to reflect that timeout_ms and retry_attempts are now actually implemented and usable in ApiConfig (D-08, D-09). This fixes the documentation drift that existed before this phase.

Purpose: Contributors and users reading CLAUDE.md will see correct information about what configuration options actually work.
Output: Updated CLAUDE.md with corrected information about API configuration options.
</objective>

<execution_context>
@/Users/hesheng/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-close-confidence-gaps-runtime-behavior/02-CONTEXT.md
@.planning/phases/02-close-confidence-gaps-runtime-behavior/02-RESEARCH.md
@CLAUDE.md
@src/config/types.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md configuration section to reflect actual implementation</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md, src/config/types.rs</read_first>
  <action>
1. Find the "Configuration Extension Pattern" section in CLAUDE.md.
2. After that section, add (or update if existing) a subsection documenting the existing `api` configuration section, specifically:
   - `timeout_ms`: API request timeout in milliseconds, default: 5000
   - `retry_attempts`: Number of retry attempts on failure, default: 2
3. The documentation needs to clearly state these options are now actually implemented and used (the drift is fixed).
4. Follow the existing pattern in CLAUDE.md showing the struct fields with defaults.
5. Make only the targeted update — do NOT do a full rewrite of documentation (D-08). Only the section about API configuration needs to reflect the actual implementation that now exists after phase changes.
6. Keep all other content unchanged per D-08 (only targeted updates where drift is known to exist).
</action>
  <verify>
    <automated>grep -E "timeout_ms|retry_attempts" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "timeout_ms"
    - CLAUDE.md contains "retry_attempts"
    - CLAUDE.md lists default 5000 for timeout_ms
    - CLAUDE.md lists default 2 for retry_attempts
    - Documentation correctly reflects that these options are configurable now
    - No other sections of CLAUDE.md were changed unnecessarily
  </acceptance_criteria>
  <done>
CLAUDE.md documentation updated. The config/runtime documentation drift is fixed. Documentation matches actual implementation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed — documentation update only. No security impact.

## STRIDE Threat Register

No threats applicable to documentation changes.
</threat_model>

<verification>
Verify that the update is minimal and targeted, only fixing the known drift.
</verification>

<success_criteria>
- CLAUDE.md updated to reflect that timeout_ms and retry_attempts are configurable (D-09)
- Only targeted update per D-08 — no full documentation rewrite
- Documentation matches actual implementation after phase changes
- Existing structure and content preserved except for the specific drift fix
</success_criteria>

<output>
After completion, create `.planning/phases/02-close-confidence-gaps-runtime-behavior/02-03-SUMMARY.md
</output>
